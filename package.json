{
  "name": "hospitalrun_v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "yarn run clean && tsc",
    "test": "yarn run compile && cypress open",
    "headless": "yarn run compile && cypress run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.11",
    "cypress": "^9.1.0",
    "cypress-xpath": "^1.6.2",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "moment": "^2.29.1",
    "rimraf": "^3.0.2"
  }
}
